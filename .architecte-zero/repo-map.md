# Repository map
- `components/`, `contexts/`, and `services/`: learner experience and mission orchestration.
- `contracts/` and `broker/`: versioned receipts, admission, and run state.
- `tests/`, `scripts/`, and `docs/`: executable and public proof surfaces.
